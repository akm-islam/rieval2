import React, { Component } from "react";
import "./style.less";
import { Point, Rect, Actions, ArchorType } from "../utils/rotateResize";
declare const defaultProps: {};
export declare type DragableProps = {
    rect: Rect;
    onChange: (rect: Rect) => void;
    children: (rect: Rect) => React.ReactNode;
} & Partial<typeof defaultProps>;
export interface DragableState {
}
declare class Dragable extends Component<DragableProps, DragableState> {
    static defaultProps: {};
    thisNode: Element | Text | null;
    action: Actions;
    mouseStart: Point;
    rectStart: Rect;
    fixedRatio: boolean;
    archor: ArchorType;
    componentWillUnmount(): void;
    /**
     * 组件拖拽（进行中处理）
     *
     * @type {EventListener}
     * @memberof Dragable
     */
    handleDragMove: EventListener;
    /**
     * 组件拖拽 (开始)
     *
     * @memberof Dragable
     */
    handleDragStart: (e: React.MouseEvent<HTMLElement, MouseEvent>, action: Actions) => void;
    /**
     * 组件拖拽 (开始结束)
     *
     * @memberof Dragable
     */
    handleDragStop: () => void;
    render(): JSX.Element;
}
export default Dragable;
